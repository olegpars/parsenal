# Missed meanings

Why a meaning missed: task, rejected direction, reason -- one line each. Not "the text was bad overall," but a specific claim/direction that failed a check or was rejected by the author.

Rule for adding entries: a direction is rejected in AUDIT/SELECT -> add it here with the user's consent, with a date.

Entry format:

```
- [YYYY-MM-DD] Task: ... | Rejected direction: ... | Reason: ... (card: slug, if any)
```

## Misses

Empty -- the first entries will come from real AUDIT/SELECT sessions.
